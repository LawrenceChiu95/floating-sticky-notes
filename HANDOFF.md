# 当前交接

> 最后同步：2026-07-12。本文件只记录当前活状态；长期架构见 `docs/architecture.md`，发布闸门见 `docs/releasing.md`，设计细节见 Issue #1 的规格与计划。

## 工作位置

- 分支：`feat/issue-1-update-progress`
- 功能代码 HEAD：`0a37dfa chore(update): 移除真机诊断代码`
- 状态：未 push、未建 PR、未发布、未关闭 Issue #1。

## Issue #1 当前结果

Windows 自动更新的非阻塞下载进度窗口已经实现。窗口独立于便签，可拖动、最小化并从任务栏恢复；下载期间便签和托盘继续工作。更新控制器使用显式阶段和 operation ID 防止迟到事件、重复下载与重复安装提示，关闭全部便签后 Windows 仍由托盘常驻。

真机故障的根因不是 updater 没有发送进度，而是 `sandbox: true` 的进度 preload 被构建为 ESM，导致 bridge 没有执行。当前构建强制所有 preload 输出 CommonJS，主窗口加载 `preload.cjs`，进度窗口加载 `updateProgressPreload.cjs`。临时诊断代码已经删除。

## 已有验证证据

- Windows 11、Node 22.11.0、npm 10.9.0。
- 本地限速更新持续 49.002 秒，进度从 2% 连续更新到 100%，不再从“正在准备”直接跳到完成。
- 已验证进度窗口最小化与任务栏恢复；下载完成后出现安装确认，选择“稍后安装”后应用继续运行并清理进度状态。
- 使用隔离 profile，未修改用户现有便签数据。
- Codex 与 Opus 已审查 `39fd093..0a37dfa`，没有高、中、低等级发现。
- 2026-07-12 本地验证：37 个测试文件、176 个测试通过；`npm run build`、`npm audit --omit=dev`、`git diff --check` 均通过。
- 最终内部 Windows 候选包为 `release/StickyNotes-Setup-0.1.10.exe`，SHA-256 为 `4c4eea83f4493fe34842144ba7dc8154014e24a70942db048d74cc68f3023b45`。asar 只包含两个 `.cjs` preload，主进程引用正确且不含临时诊断标记。

这个 `0.1.10` 只用于内部验证。公开 `0.1.10` 已经存在，正式发布不得覆盖或重发同一版本号。

## 尚未验证

- 正式在线更新源的完整更新闭环。
- 真实 `0.1.10 -> 0.1.11` 覆盖安装及便签、图片保留。
- 下载中断网后的错误清理与再次检查。
- 多显示器以及 100%、125%、150% DPI。
- 用户点击进度窗口关闭按钮时确实无法关闭。

## 下一窗口

用户已选择在托盘增加“关于悬浮便签”：点击后使用系统信息框显示通过 `app.getVersion()` 取得的实际安装版本。这个需求尚未写设计、实现或测试，不得标记为完成。

建议顺序：

1. 完成“关于悬浮便签”的最小设计、实现、测试与 Codex/Opus review。
2. 将版本升到 `0.1.11`，同步 lockfile，并把 `CHANGELOG.md` 的未发布条目归档到正式版本。
3. 构建最终候选包，通过指向 Windows 远程机器的 XDMaker 会话完成上述剩余真机检查，优先验证真实覆盖安装和本地数据保留。
4. 所有闸门通过后再询问用户是否 push、创建 PR 和发布；没有明确许可时不得执行这些操作。
