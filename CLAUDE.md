# Claude Code 项目入口

项目开发规则的单一真源是 `AGENTS.md`，启动后先读取并遵守，不在本文件复制规则。若本地存在 `HANDOFF.md`，一并读取当前活状态和未同步队列。

只要本轮涉及需求、Bug、用户反馈、实现、验证、版本或发布，还必须读取 `docs/project-management.md`。

用户不负责维护分类和状态。AI 应在对话过程中主动维护项目上下文，并只在协议规定的产品取舍、信息不足、对外发布或不可逆操作时询问。
